let tid=null;async function startScan(){let source=document.getElementById('source').value||'/dev/mock';let r=await fetch('/api/scan',{method:'POST',body:JSON.stringify({source})});let j=await r.json();tid=j.task_id;poll();}
async function poll(){if(!tid)return;let r=await fetch('/api/task/'+tid);let t=await r.json();document.getElementById('status').innerText=t.status+' '+t.progress+'%';document.getElementById('bar').style.width=t.progress+'%';document.getElementById('log').innerText=(t.logs||[]).join('\n');let ul=document.getElementById('files');ul.innerHTML='';((t.result||{}).files||[]).forEach(f=>{let li=document.createElement('li');li.innerText=f.name+' '+f.size+'b';ul.appendChild(li)});if(t.status==='running'||t.status==='pending')setTimeout(poll,400)}
